{
  "devDependencies": {
    "vuepress": "^1.7.1",
    "vuepress-theme-meteorlxy": "^1.9.0"
  },
  "scripts": {
    "dev": "vuepress dev src && netlify-lambda build functions/",
    "build": "vuepress build src --dest dist && netlify-lambda build functions/"
  }
}
